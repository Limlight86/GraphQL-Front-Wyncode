import React from 'react';
import { SectionWrapper, List } from '../../components';

const MovieList = _ => (
  <SectionWrapper>
    <List movies={[]} />
  </SectionWrapper>
);

export default MovieList;
